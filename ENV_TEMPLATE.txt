# Copy this file to .env and fill in your values

# MongoDB Connection (REQUIRED)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/party-game?retryWrites=true&w=majority

# Server Port (Render will set this automatically)
PORT=5001

# Email Configuration (Optional - only if using email features)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

